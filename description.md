Scan your ebook and audiobook library and compare it against MyAnonamouse (via Prowlarr) to find what you already have, what might be missing, and what partially matches.

Supports multithreading, caching for fast re-runs, smart fuzzy matching (title/author/series), and resume capability that skips previously matched items.

Outputs a detailed CSV report with match confidence and best results.
